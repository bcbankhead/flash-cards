extends layout

block content
  h1 Login
  p Welcome to #{title}
  if message
    |#{message}
    
  if user
      p
        |Hello #{user.displayName}
        ||
        a(href="/logout") Logout
    else
      p
        a(href="/auth/linkedin") Login

  if profile
    img(src=profile.pictureUrl)
